# CatBreak 需求规格说明书

> 版本: 1.6.0 | 反向推导 | 2026-05-25

---

## 1. 产品概述

### 1.1 产品定位

CatBreak 是一款 macOS 菜单栏工具，通过全屏覆盖休息提醒强制用户定时休息，防止长时间持续用眼和久坐。以猫咪为主题，提供沉浸式休息体验。

### 1.2 目标用户

- 长时间使用电脑的办公人群
- 程序员、设计师、文字工作者等需要屏幕前工作的职业
- 关注健康但缺乏自制力的用户

### 1.3 核心价值

- **强制休息**: 全屏覆盖不可跳过，真正打断连续用眼
- **智能感知**: 检测会议/通话等场景，推迟休息避免干扰工作
- **猫咪陪伴**: 萌猫主题 + 名言警句，让休息更有趣
- **零打扰配置**: 菜单栏常驻，不占 Dock，设置简洁

---

## 2. 功能需求

### F1. 菜单栏常驻

| 项目 | 说明 |
|------|------|
| **图标** | 猫咪剪影 SF Symbol (`cat.fill`) |
| **左键点击** | 弹出设置面板 (Popover, 320×600pt) |
| **右键点击** | 上下文菜单: "打开设置" / "退出" |
| **Popover 行为** | 瞬时模式 (点击外部自动关闭), 带动画 |
| **Dock 隐藏** | `LSUIElement = YES`, 不显示 Dock 图标, 不出现 Cmd+Tab 切换列表 |

### F2. 使用时长计时

#### F2.1 计时状态机

```
                  用户活跃
    ┌─────────┐  ───────>  ┌────────────┐  达到上限  ┌──────────┐
    │  idle   │            │ monitoring  │ ────────> │ breaking │
    │  待机   │ <───────  │   监控中    │           │  休息中  │
    └─────────┘  离开>10分 └────────────┘           └──────────┘
                        ↑        │                       │
                        │  离开>3分│                       │ 倒计时归零
                        │        ↓                       ↓
                        └── ┌──────────┐          ┌──────────┐
                            │  paused   │          │  idle    │
                            │  已离开    │          │  待机    │
                            └──────────┘          └──────────┘
```

- **idle (待机)**: 等待用户活动, 不累计时间
- **monitoring (监控中)**: 每 1 秒滴答, 累计使用时长
- **paused (已离开)**: 无输入超过 3 分钟, 暂停计时
- **breaking (休息中)**: 全屏覆盖显示, 倒计时休息时长

#### F2.2 空闲检测

| 规则 | 阈值 | 行为 |
|------|------|------|
| 进入暂停 | 无鼠标/键盘输入 > 3 分钟 | 计时暂停, 状态变为 paused |
| 恢复监控 | 离开后 < 10 分钟恢复输入 | 从暂停时的时长继续计时 |
| 重置计时 | 离开 > 10 分钟 | 已用时长清零, 回到 idle |

输入事件包括: mouseMoved, leftMouseDown, rightMouseDown, keyDown, scrollWheel

#### F2.3 使用时长上限

- 范围: 60 ~ 7200 秒 (1 分钟 ~ 120 分钟)
- 步长: 30 秒
- 默认: 3600 秒 (60 分钟)
- UI 显示: "X分钟" 或 "X分Y秒"

#### F2.4 休息时长

- 范围: 10 ~ 1200 秒 (10 秒 ~ 20 分钟)
- 步长: 10 秒
- 默认: 300 秒 (5 分钟)
- UI 显示: "X分钟" 或 "X分Y秒"

### F3. 全屏休息覆盖

#### F3.1 窗口层级

- 窗口层级: `CGShieldingWindowLevel()` (屏幕保护层级, 高于所有应用)
- 风格: 无边框, 透明背景, 无阴影, 忽略鼠标事件
- 空间行为: 加入所有 Space, fullScreenAuxiliary, stationary (跟随 Mission Control 切换)
- 多显示器: 在每块屏幕创建独立覆盖窗口

#### F3.2 覆盖界面

```
┌──────────────────────────────────────────────────┐
│              [猫咪背景图 / 深色渐变]               │
│                                                  │
│              [45% 黑色半透明遮罩]                  │
│                                                  │
│                ● 装饰光晕 (白/绿)                 │
│                                                  │
│            🐱 猫咪图标 (55pt, 投影)               │
│                                                  │
│          "放下键盘，起来活动一下吧！"                │
│            [随机名言警句, 衬线斜体]                 │
│                                                  │
│            ┌─────────────────┐                   │
│            │   第 N 次休息    │  (N>1 时显示)      │
│            └─────────────────┘                   │
│                                                  │
│              该休息啦！                            │
│                                                  │
│          ┌───────────────────┐                   │
│          │      04:59        │  (72pt 等宽, 圆角卡片)│
│          └───────────────────┘                   │
│                                                  │
│        伸展一下 · 看看远处 · 深呼吸                │
│                                                  │
│  [最后5秒: 黄字红底, 1.05x 放大, 脉冲动画]         │
│  [文字变为: "马上就可以继续啦！"]                   │
└──────────────────────────────────────────────────┘
```

#### F3.3 倒计时结束

- 自动关闭所有覆盖窗口
- 状态回到 idle
- 休息次数 (breakCount) +1

### F4. 名言警句

- 数据源: `catbreak.txt` (5200+ 行)
- 解析规则: 跳过标题行 (如 "名言名句警句摘抄大全【一】")、URL 行
- 匹配格式: `数字、警句内容`
- 每次休息随机抽取一条显示
- 兜底文案: "放下键盘，起来活动一下吧！"

### F5. 会议/通话智能检测 (敏感应用)

#### F5.1 敏感应用列表

默认预置 7 款应用:

| 应用 | Bundle ID |
|------|-----------|
| 腾讯会议 | com.tencent.meeting |
| Zoom | us.zoom.xos |
| Microsoft Teams | com.microsoft.teams |
| Chrome | com.google.Chrome |
| FaceTime | com.apple.FaceTime |
| QQ | com.tencent.MacQQ |
| 微信 | com.tencent.xinWeChat |

用户可自定义增删。

#### F5.2 检测策略

检测周期: 监控中每 10 秒, 休息中每 5 秒

**1. 应用分类:**
- 会议类: 腾讯会议、Zoom、Teams、FaceTime
- 通讯类: 微信、QQ
- 其他: 用户添加的任意应用

**2. 窗口标题关键词匹配 (优先级最高):**

中文关键词: "会议中"、"会议进行中"、"正在开会"、"视频中"、"音频中"、"共享屏幕"、"邀请你加入"、"语音通话"、"视频通话"、"通话中"

英文关键词: "Meeting in Progress"、"In Meeting"、"Screen Sharing"、"Zoom Meeting"、"Microsoft Teams Meeting"、"Calling"、"Voice Call"

**3. 麦克风状态检测:**
- CoreAudio API 查询 `kAudioDevicePropertyDeviceIsRunningSomewhere`
- 检测默认输入设备是否正在使用
- 检测特定应用进程是否占用麦克风

**4. 窗口启发式规则:**
- 腾讯会议: 窗口数 ≥ 2 且至少一个 > 600×400
- Zoom: 窗口数 ≥ 2 且一个带标题窗口 > 300×200
- 微信: 检测小尺寸浮动窗口 (50-300 × 50-250, layer 0) 表示语音通话

#### F5.3 推迟机制

| 条件 | 行为 |
|------|------|
| 检测到敏感应用活跃 | 推迟休息 (最多 600 秒 / 10 分钟) |
| 推迟期间 | 计时继续, 设置面板显示橙色警告: "会议/视频中，推迟休息" |
| 推迟超过 10 分钟 | 强制执行休息 |
| 休息中检测到敏感应用 | 提前结束休息 |

### F6. 音量控制

| 场景 | 行为 |
|------|------|
| 休息开始 (muteOnBreak=ON) | AppleScript 保存当前音量 → 静音 |
| 休息结束 (muteOnBreak=ON) | AppleScript 关闭静音 → 恢复原音量 |
| muteOnBreak=OFF | 不进行任何音量操作 |
| 异步执行 | 在 global utility queue 执行, 不阻塞 UI |

### F7. 开机自启

- 开关: "开机自启" Toggle
- 实现: `SMAppService.mainApp` API (macOS 13+)
- 失败时弹窗提示: "设置失败"
- AppDelegate 中 `applicationDidFinishLaunching` 调用 `SMAppService.mainApp.register()` 完成注册

### F8. 应用选择器

- 扫描目录: `/Applications`, `~/Applications`, `/System/Applications`
- 读取每个 .app 的 Info.plist: Bundle ID, 显示名称, 图标
- 按 Bundle ID 去重, 按名称字母排序
- 搜索: 按名称或 Bundle ID 实时过滤
- 每行显示: 32×32 应用图标, 名称, Bundle ID, 勾选指示
- 手动输入 Bundle ID (可折叠区域)
- "完成" 按钮确认选择
- 窗口大小: 400×500pt

### F9. 设置面板 UI

```
┌────────────────────────────────────┐
│                                    │
│           01:23:45                 │  ← 会话计时 (36pt 等宽)
│  ████████████░░░░░░░░  56%        │  ← 进度条 (蓝/灰/红)
│  ● 监控中                         │  ← 状态标签 (带色点)
│                                    │
│  使用时长上限  ──────●──  60分钟    │  ← TimeSlider
│  休息时长      ────●────  5分钟     │  ← TimeSlider
│                                    │
│  🔊 休息时静音       [Toggle]      │
│  ⚡ 开机自启         [Toggle]      │
│                                    │
│  🟠 敏感应用 (3)          ▼       │  ← 可折叠区域
│    📹 腾讯会议          [−]      │
│    📹 Zoom              [−]      │
│    💬 微信              [−]      │
│    [+ 选择应用]                   │
│                                    │
│  ❌ 退出                          │
└────────────────────────────────────┘
```

---

## 3. 非功能需求

### 3.1 性能

- 全局事件监控不显著影响系统性能
- 敏感应用检测异步执行, 不阻塞主线程
- 应用扫描在后台线程 (`qos: .userInitiated`)

### 3.2 兼容性

- macOS 13.0 (Ventura) 及以上
- Intel + Apple Silicon (Universal Binary)
- 多显示器支持

### 3.3 安全性

- App Sandbox: 关闭 (需要监控其他应用窗口)
- Hardened Runtime: 开启
- 代码签名: 临时签名 (ad-hoc)
- 无网络请求, 无数据上报

### 3.4 本地化

- UI 语言: 简体中文
- 名言警句: 中文

### 3.5 持久化

- 所有设置通过 `UserDefaults` 存储
- 存储键:

| Key | 类型 | 默认值 |
|-----|------|--------|
| usageLimitSeconds | Int | 3600 |
| breakDurationSeconds | Int | 300 |
| launchAtLogin | Bool | false |
| muteOnBreak | Bool | false |
| sensitiveAppBundleIds | [String] | [7 个预置] |

---

## 4. 用户故事

| ID | 作为 | 我想要 | 以便 |
|----|------|--------|------|
| US1 | 办公人员 | 在使用电脑一定时间后强制休息 | 保护视力和身体健康 |
| US2 | 办公人员 | 自定义使用时长上限和休息时长 | 适应个人工作节奏 |
| US3 | 办公人员 | 在全屏休息时看到有趣的猫咪和名言 | 让休息不那么讨厌 |
| US4 | 会议参与者 | 在开会时自动推迟休息 | 不影响重要工作 |
| US5 | 多显示器用户 | 休息覆盖在所有屏幕上同时显示 | 无法绕过休息 |
| US6 | 离开工位的用户 | 自动暂停计时 | 不浪费计时额度 |
| US7 | 办公室用户 | 休息开始时自动静音 | 不打扰同事 |
| US8 | 所有用户 | 开机自动启动 | 不用每次手动打开 |
| US9 | 定制用户 | 添加自定义应用到敏感列表 | 适应自己的工作场景 |

---

## 5. 技术栈

| 层面 | 技术选型 |
|------|----------|
| 语言 | Swift 5.9 |
| UI 框架 | SwiftUI (覆盖层、设置面板), AppKit (窗口管理、菜单栏) |
| 构建 | Xcode 15.0 + SPM |
| 最低系统 | macOS 13.0 |
| 测试 | Swift Testing (仅占位) |

---

## 6. 已知局限

- 不含音效 (无猫咪叫声等)
- 无通知中心集成 (不依赖系统通知, 确保休息覆盖不被忽略)
- 无键盘快捷键 (仅标准 Cmd+Q)
- 无 AppleScript 接口 / URL Scheme
- 无休息动画 (Gen 2 移除了 Gen 1 的 spring 动画)
- 无无障碍设施 (仅菜单栏按钮有 accessibilityDescription)
- 无分析统计 (无使用时长报表)
